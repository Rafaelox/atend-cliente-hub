name: atend-cliente-hub
services:
  - type: app
    data:
      serviceName: atend-cliente-hub
      source:
        type: git
        repo: https://github.com/seu-usuario/seu-repo.git  # Substitua pela sua URL do repositório
        branch: main
      buildCommand: |
        npm ci
        npm run build
      startCommand: |
        npx serve -s dist -l 80
      domains:
        - host: yourdomain.com  # Substitua pelo seu domínio
      env:
        - key: NODE_ENV
          value: production
        - key: VITE_SUPABASE_URL
          value: https://mmqorugxbsspuyqlraia.supabase.co
        - key: VITE_SUPABASE_ANON_KEY
          value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1tcW9ydWd4YnNzcHV5cWxyYWlhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTE5MDM3MjYsImV4cCI6MjA2NzQ3OTcyNn0.8e3ohcVXPJVBvtw82aKmvAsCpf_8dfOjaB6U2g-hCTE
      mounts: []
      ports:
        - published: 80
          target: 80
      deploy:
        replicas: 1
        resources:
          reservations:
            memory: 256M
          limits:
            memory: 512M
        restartPolicy:
          condition: unless-stopped